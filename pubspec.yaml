name: to_do
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
   sdk: '>=3.5.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  path: ^1.9.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
